<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.FavoriteMapper">

    <resultMap id="FavoriteMap" type="com.example.demo.model.Favorite">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="goodsId" column="goods_id"/>
    </resultMap>

    <insert id="addFavorite">
        INSERT INTO favorites(user_id, goods_id) VALUES(#{userId}, #{goodsId})
    </insert>

    <delete id="removeFavorite">
        DELETE FROM favorites WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </delete>

    <select id="selectFavoriteGoodsIds" resultType="java.lang.Integer">
        SELECT goods_id FROM favorites WHERE user_id = #{userId}
    </select>

</mapper>